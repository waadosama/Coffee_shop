import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand palette inspired by RWAQ Speciality Coffee
  static const Color creamBackground = Color(0xFFF5EBE1);
  static const Color warmCream = Color(0xFFEFE3D3);
  static const Color cardSurface = Color(0xFFFAF5EE);
  
  // RWAQ Terracotta Rust Red / Orange (from menu title & header)
  static const Color terracotta = Color(0xFFD8533B);
  static const Color terracottaDark = Color(0xFFB83C26);
  static const Color terracottaLight = Color(0xFFE87661);

  // Dark Coffee & Charcoal (from text, mascot, and cup logos)
  static const Color darkCoffee = Color(0xFF2B2523);
  static const Color coffeeLight = Color(0xFF4A423F);
  static const Color textMuted = Color(0xFF7A706A);

  // RWAQ Signature Accent Green (from signature star icons & matcha drinks)
  static const Color signatureGreen = Color(0xFF2E6F40);
  static const Color signatureGreenLight = Color(0xFFE4F0E8);

  // Golden / Amber Hour (from iced passionfruit drinks & golden hour photo)
  static const Color goldenHour = Color(0xFFE2A03F);
  static const Color goldenLight = Color(0xFFFFF4DF);
}
